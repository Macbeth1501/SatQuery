# SatQuery AI frontend configuration. Copy to .env.local (git-ignored via *.local).
# URL of the backend API, with no trailing path. Default: http://127.0.0.1:8000
# VITE_API_BASE_URL=http://127.0.0.1:8000
